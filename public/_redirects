# Cloudflare Pages redirects.
# Populated in Phase 4 from redirects.json — every legacy URL (both /page.html
# and /page forms) gets a 301 to its successor. Dispositions live in
# content/legacy/URL-INVENTORY.md and are owner-approved before landing here.
